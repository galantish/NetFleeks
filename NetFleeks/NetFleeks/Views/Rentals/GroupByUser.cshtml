@model IEnumerable<NetFleeks.ViewModel.RentalViewModel>


<!--  jQuery -->
<script type="text/javascript" src="https://code.jquery.com/jquery-1.11.3.min.js"></script>

<!-- Isolated Version of Bootstrap, not needed if your site already uses Bootstrap -->
<link rel="stylesheet" href="https://formden.com/static/cdn/bootstrap-iso.css" />

<!-- Bootstrap Date-Picker Plugin -->
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/js/bootstrap-datepicker.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/css/bootstrap-datepicker3.css" />

<link rel="stylesheet" href="~/Content/bootstrap.css" />

@{
    ViewBag.Title = "Rentals";
}

<h2>Rentals</h2>

<p style="font-size:17px">
    @Html.ActionLink("All times Rentals", "IndexAll")&nbsp &nbsp &nbsp &nbsp
    @Html.ActionLink("Current Rentals", "Index")&nbsp &nbsp &nbsp &nbsp
    @Html.ActionLink("Group By Users", "GroupBy1")&nbsp &nbsp &nbsp &nbsp
    @Html.ActionLink("Group By Genres", "GroupBy2")&nbsp &nbsp &nbsp &nbsp


</p>

@foreach (var user in Model.GroupBy(g => g.rentalUser))
{

    <table class="table table-hover table-bordered">
        <thead>
            <tr><th width="60%">@Html.DisplayFor(modelItem => user.Key)</th></tr>

            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.movie)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.genre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.rentalExpiration)
                </th>
            </tr>
        </thead>
        @foreach (var item in user)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.movie)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.genre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.rentalExpiration)
                </td></tr>
        }

    </table>
}