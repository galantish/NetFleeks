@model IEnumerable<NetFleeks.Models.Rentals>

<!--  jQuery -->
<script type="text/javascript" src="https://code.jquery.com/jquery-1.11.3.min.js"></script>

<!-- Isolated Version of Bootstrap, not needed if your site already uses Bootstrap -->
<link rel="stylesheet" href="https://formden.com/static/cdn/bootstrap-iso.css" />

<!-- Bootstrap Date-Picker Plugin -->
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/js/bootstrap-datepicker.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/css/bootstrap-datepicker3.css" />

<link rel="stylesheet" href="~/Content/Search.css" />

@{
    ViewBag.Title = "Rentals";
}

<h2 id="pageTitle">Rentals</h2>
<br />
<p style="font-size:17px; font-weight: bold; font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif">
   @Html.ActionLink("Rent a New Movie", "Index", "Movies")&nbsp &nbsp &nbsp &nbsp
   @Html.ActionLink("All time Rentals", "IndexAll")&nbsp &nbsp &nbsp &nbsp
   @Html.ActionLink("Current Rentals", "Index")

</p>

</br>

@if (!Model.Any())
{
    <p>You Have Not Rented a Movie Yet.</p>
}
else
{
    <table class="table" id="rentalsTable">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.rentalMovie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.rentalExpiration)
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td> 
                    @Html.DisplayFor(modelItem => item.rentalMovie)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.rentalExpiration)
                </td>
            </tr>
        }
   
    </table>
}
